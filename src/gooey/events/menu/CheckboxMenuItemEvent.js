import Event from "../Event.js";

/**
 * Event constants for CheckboxMenuItem component
 */
export default class CheckboxMenuItemEvent extends Event {
    static CHANGE = "change";

    constructor() {
        super();
    }
}