import Event from "../Event.js";

/**
 * Event constants for WaffleMenuItem component
 */
export default class WaffleMenuItemEvent extends Event {
    static SELECT = "select";

    constructor() {
        super();
    }
}
