import Event from "../Event.js";

/**
 * Event constants for Checkbox component
 */
export default class CheckboxEvent extends Event {
    static CHANGE = "change";

    constructor() {
        super();
    }
}