/**
 * Constants for tooltip animation types.
 * Used with the `animation` attribute on tooltip components.
 */
export default class TooltipAnimation {
    static FADE         = "fade";
    static SCALE        = "scale";
    static SHIFT_AWAY   = "shift-away";
    static SHIFT_TOWARD = "shift-toward";
    static NONE         = "none";
}
